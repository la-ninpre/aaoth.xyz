title: aaoth

# exclude non-site files
exclude:
  - LICENSE
  - README.md

defaults:
  -
    scope:
      path: ""
    values:
      layout: "default"
  -
    scope:
      path: "_posts"
    values:
      layout: "post"
